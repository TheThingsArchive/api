/*
 * DO NOT EDIT.
 *
 * Generated by the protocol buffer compiler.
 * Source: github.com/TheThingsNetwork/api/router/router.proto
 *
 */

/*
 * Copyright 2017, gRPC Authors All rights reserved.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
import Foundation
import Dispatch
import gRPC

/// Type for errors thrown from generated client code.
internal enum Router_RouterClientError : Error {
  case endOfStream
  case invalidMessageReceived
  case error(c: CallResult)
}

/// GatewayStatus (Client Streaming)
internal class Router_RouterGatewayStatusCall {
  private var call : Call

  /// Create a call.
  fileprivate init(_ channel: Channel) {
    self.call = channel.makeCall("/router.Router/GatewayStatus")
  }

  /// Call this to start a call. Nonblocking.
  fileprivate func start(metadata:Metadata, completion:@escaping (CallResult)->())
    throws -> Router_RouterGatewayStatusCall {
      try self.call.start(.clientStreaming, metadata:metadata, completion:completion)
      return self
  }

  /// Call this to send each message in the request stream. Nonblocking.
  internal func send(_ message:Gateway_Status, errorHandler:@escaping (Error)->()) throws {
    let messageData = try message.serializedData()
    try call.sendMessage(data:messageData, errorHandler:errorHandler)
  }

  /// Call this to close the connection and wait for a response. Blocking.
  internal func closeAndReceive() throws -> Google_Protobuf_Empty {
    var returnError : Router_RouterClientError?
    var returnResponse : Google_Protobuf_Empty!
    let sem = DispatchSemaphore(value: 0)
    do {
      try closeAndReceive() {response, error in
        returnResponse = response
        returnError = error
        sem.signal()
      }
      _ = sem.wait(timeout: DispatchTime.distantFuture)
    } catch (let error) {
      throw error
    }
    if let returnError = returnError {
      throw returnError
    }
    return returnResponse
  }

  /// Call this to close the connection and wait for a response. Nonblocking.
  internal func closeAndReceive(completion:@escaping (Google_Protobuf_Empty?, Router_RouterClientError?)->())
    throws {
      do {
        try call.receiveMessage() {(responseData) in
          if let responseData = responseData,
            let response = try? Google_Protobuf_Empty(serializedData:responseData) {
            completion(response, nil)
          } else {
            completion(nil, Router_RouterClientError.invalidMessageReceived)
          }
        }
        try call.close(completion:{})
      } catch (let error) {
        throw error
      }
  }
}

/// Uplink (Client Streaming)
internal class Router_RouterUplinkCall {
  private var call : Call

  /// Create a call.
  fileprivate init(_ channel: Channel) {
    self.call = channel.makeCall("/router.Router/Uplink")
  }

  /// Call this to start a call. Nonblocking.
  fileprivate func start(metadata:Metadata, completion:@escaping (CallResult)->())
    throws -> Router_RouterUplinkCall {
      try self.call.start(.clientStreaming, metadata:metadata, completion:completion)
      return self
  }

  /// Call this to send each message in the request stream. Nonblocking.
  internal func send(_ message:Router_UplinkMessage, errorHandler:@escaping (Error)->()) throws {
    let messageData = try message.serializedData()
    try call.sendMessage(data:messageData, errorHandler:errorHandler)
  }

  /// Call this to close the connection and wait for a response. Blocking.
  internal func closeAndReceive() throws -> Google_Protobuf_Empty {
    var returnError : Router_RouterClientError?
    var returnResponse : Google_Protobuf_Empty!
    let sem = DispatchSemaphore(value: 0)
    do {
      try closeAndReceive() {response, error in
        returnResponse = response
        returnError = error
        sem.signal()
      }
      _ = sem.wait(timeout: DispatchTime.distantFuture)
    } catch (let error) {
      throw error
    }
    if let returnError = returnError {
      throw returnError
    }
    return returnResponse
  }

  /// Call this to close the connection and wait for a response. Nonblocking.
  internal func closeAndReceive(completion:@escaping (Google_Protobuf_Empty?, Router_RouterClientError?)->())
    throws {
      do {
        try call.receiveMessage() {(responseData) in
          if let responseData = responseData,
            let response = try? Google_Protobuf_Empty(serializedData:responseData) {
            completion(response, nil)
          } else {
            completion(nil, Router_RouterClientError.invalidMessageReceived)
          }
        }
        try call.close(completion:{})
      } catch (let error) {
        throw error
      }
  }
}

/// Subscribe (Server Streaming)
internal class Router_RouterSubscribeCall {
  private var call : Call

  /// Create a call.
  fileprivate init(_ channel: Channel) {
    self.call = channel.makeCall("/router.Router/Subscribe")
  }

  /// Call this once with the message to send. Nonblocking.
  fileprivate func start(request: Router_SubscribeRequest,
                         metadata: Metadata,
                         completion: @escaping (CallResult) -> ())
    throws -> Router_RouterSubscribeCall {
      let requestData = try request.serializedData()
      try call.start(.serverStreaming,
                     metadata:metadata,
                     message:requestData,
                     completion:completion)
      return self
  }

  /// Call this to wait for a result. Blocking.
  internal func receive() throws -> Router_DownlinkMessage {
    var returnError : Router_RouterClientError?
    var returnResponse : Router_DownlinkMessage!
    let sem = DispatchSemaphore(value: 0)
    do {
      try receive() {response, error in
        returnResponse = response
        returnError = error
        sem.signal()
      }
      _ = sem.wait(timeout: DispatchTime.distantFuture)
    }
    if let returnError = returnError {
      throw returnError
    }
    return returnResponse
  }

  /// Call this to wait for a result. Nonblocking.
  internal func receive(completion:@escaping (Router_DownlinkMessage?, Router_RouterClientError?)->()) throws {
    do {
      try call.receiveMessage() {(responseData) in
        if let responseData = responseData {
          if let response = try? Router_DownlinkMessage(serializedData:responseData) {
            completion(response, nil)
          } else {
            completion(nil, Router_RouterClientError.invalidMessageReceived)
          }
        } else {
          completion(nil, Router_RouterClientError.endOfStream)
        }
      }
    }
  }
}

/// Activate (Unary)
internal class Router_RouterActivateCall {
  private var call : Call

  /// Create a call.
  fileprivate init(_ channel: Channel) {
    self.call = channel.makeCall("/router.Router/Activate")
  }

  /// Run the call. Blocks until the reply is received.
  fileprivate func run(request: Router_DeviceActivationRequest,
                       metadata: Metadata) throws -> Router_DeviceActivationResponse {
    let sem = DispatchSemaphore(value: 0)
    var returnCallResult : CallResult!
    var returnResponse : Router_DeviceActivationResponse?
    _ = try start(request:request, metadata:metadata) {response, callResult in
      returnResponse = response
      returnCallResult = callResult
      sem.signal()
    }
    _ = sem.wait(timeout: DispatchTime.distantFuture)
    if let returnResponse = returnResponse {
      return returnResponse
    } else {
      throw Router_RouterClientError.error(c: returnCallResult)
    }
  }

  /// Start the call. Nonblocking.
  fileprivate func start(request: Router_DeviceActivationRequest,
                         metadata: Metadata,
                         completion: @escaping (Router_DeviceActivationResponse?, CallResult)->())
    throws -> Router_RouterActivateCall {

      let requestData = try request.serializedData()
      try call.start(.unary,
                     metadata:metadata,
                     message:requestData)
      {(callResult) in
        if let responseData = callResult.resultData,
          let response = try? Router_DeviceActivationResponse(serializedData:responseData) {
          completion(response, callResult)
        } else {
          completion(nil, callResult)
        }
      }
      return self
  }
}

/// Call methods of this class to make API calls.
internal class Router_RouterService {
  private var channel: Channel

  /// This metadata will be sent with all requests.
  internal var metadata : Metadata

  /// This property allows the service host name to be overridden.
  /// For example, it can be used to make calls to "localhost:8080"
  /// appear to be to "example.com".
  internal var host : String {
    get {
      return self.channel.host
    }
    set {
      self.channel.host = newValue
    }
  }

  /// Create a client that makes insecure connections.
  internal init(address: String) {
    gRPC.initialize()
    channel = Channel(address:address)
    metadata = Metadata()
  }

  /// Create a client that makes secure connections.
  internal init(address: String, certificates: String?, host: String?) {
    gRPC.initialize()
    channel = Channel(address:address, certificates:certificates, host:host)
    metadata = Metadata()
  }

  /// Asynchronous. Client-streaming.
  /// Use methods on the returned object to stream messages and
  /// to close the connection and wait for a final response.
  internal func gatewaystatus(completion: @escaping (CallResult)->())
    throws
    -> Router_RouterGatewayStatusCall {
      return try Router_RouterGatewayStatusCall(channel).start(metadata:metadata, completion:completion)
  }
  /// Asynchronous. Client-streaming.
  /// Use methods on the returned object to stream messages and
  /// to close the connection and wait for a final response.
  internal func uplink(completion: @escaping (CallResult)->())
    throws
    -> Router_RouterUplinkCall {
      return try Router_RouterUplinkCall(channel).start(metadata:metadata, completion:completion)
  }
  /// Asynchronous. Server-streaming.
  /// Send the initial message.
  /// Use methods on the returned object to get streamed responses.
  internal func subscribe(_ request: Router_SubscribeRequest, completion: @escaping (CallResult)->())
    throws
    -> Router_RouterSubscribeCall {
      return try Router_RouterSubscribeCall(channel).start(request:request, metadata:metadata, completion:completion)
  }
  /// Synchronous. Unary.
  internal func activate(_ request: Router_DeviceActivationRequest)
    throws
    -> Router_DeviceActivationResponse {
      return try Router_RouterActivateCall(channel).run(request:request, metadata:metadata)
  }
  /// Asynchronous. Unary.
  internal func activate(_ request: Router_DeviceActivationRequest,
                  completion: @escaping (Router_DeviceActivationResponse?, CallResult)->())
    throws
    -> Router_RouterActivateCall {
      return try Router_RouterActivateCall(channel).start(request:request,
                                                 metadata:metadata,
                                                 completion:completion)
  }
}

/// Type for errors thrown from generated client code.
internal enum Router_RouterManagerClientError : Error {
  case endOfStream
  case invalidMessageReceived
  case error(c: CallResult)
}

/// GatewayStatus (Unary)
internal class Router_RouterManagerGatewayStatusCall {
  private var call : Call

  /// Create a call.
  fileprivate init(_ channel: Channel) {
    self.call = channel.makeCall("/router.RouterManager/GatewayStatus")
  }

  /// Run the call. Blocks until the reply is received.
  fileprivate func run(request: Router_GatewayStatusRequest,
                       metadata: Metadata) throws -> Router_GatewayStatusResponse {
    let sem = DispatchSemaphore(value: 0)
    var returnCallResult : CallResult!
    var returnResponse : Router_GatewayStatusResponse?
    _ = try start(request:request, metadata:metadata) {response, callResult in
      returnResponse = response
      returnCallResult = callResult
      sem.signal()
    }
    _ = sem.wait(timeout: DispatchTime.distantFuture)
    if let returnResponse = returnResponse {
      return returnResponse
    } else {
      throw Router_RouterManagerClientError.error(c: returnCallResult)
    }
  }

  /// Start the call. Nonblocking.
  fileprivate func start(request: Router_GatewayStatusRequest,
                         metadata: Metadata,
                         completion: @escaping (Router_GatewayStatusResponse?, CallResult)->())
    throws -> Router_RouterManagerGatewayStatusCall {

      let requestData = try request.serializedData()
      try call.start(.unary,
                     metadata:metadata,
                     message:requestData)
      {(callResult) in
        if let responseData = callResult.resultData,
          let response = try? Router_GatewayStatusResponse(serializedData:responseData) {
          completion(response, callResult)
        } else {
          completion(nil, callResult)
        }
      }
      return self
  }
}

/// GetStatus (Unary)
internal class Router_RouterManagerGetStatusCall {
  private var call : Call

  /// Create a call.
  fileprivate init(_ channel: Channel) {
    self.call = channel.makeCall("/router.RouterManager/GetStatus")
  }

  /// Run the call. Blocks until the reply is received.
  fileprivate func run(request: Router_StatusRequest,
                       metadata: Metadata) throws -> Router_Status {
    let sem = DispatchSemaphore(value: 0)
    var returnCallResult : CallResult!
    var returnResponse : Router_Status?
    _ = try start(request:request, metadata:metadata) {response, callResult in
      returnResponse = response
      returnCallResult = callResult
      sem.signal()
    }
    _ = sem.wait(timeout: DispatchTime.distantFuture)
    if let returnResponse = returnResponse {
      return returnResponse
    } else {
      throw Router_RouterManagerClientError.error(c: returnCallResult)
    }
  }

  /// Start the call. Nonblocking.
  fileprivate func start(request: Router_StatusRequest,
                         metadata: Metadata,
                         completion: @escaping (Router_Status?, CallResult)->())
    throws -> Router_RouterManagerGetStatusCall {

      let requestData = try request.serializedData()
      try call.start(.unary,
                     metadata:metadata,
                     message:requestData)
      {(callResult) in
        if let responseData = callResult.resultData,
          let response = try? Router_Status(serializedData:responseData) {
          completion(response, callResult)
        } else {
          completion(nil, callResult)
        }
      }
      return self
  }
}

/// Call methods of this class to make API calls.
internal class Router_RouterManagerService {
  private var channel: Channel

  /// This metadata will be sent with all requests.
  internal var metadata : Metadata

  /// This property allows the service host name to be overridden.
  /// For example, it can be used to make calls to "localhost:8080"
  /// appear to be to "example.com".
  internal var host : String {
    get {
      return self.channel.host
    }
    set {
      self.channel.host = newValue
    }
  }

  /// Create a client that makes insecure connections.
  internal init(address: String) {
    gRPC.initialize()
    channel = Channel(address:address)
    metadata = Metadata()
  }

  /// Create a client that makes secure connections.
  internal init(address: String, certificates: String?, host: String?) {
    gRPC.initialize()
    channel = Channel(address:address, certificates:certificates, host:host)
    metadata = Metadata()
  }

  /// Synchronous. Unary.
  internal func gatewaystatus(_ request: Router_GatewayStatusRequest)
    throws
    -> Router_GatewayStatusResponse {
      return try Router_RouterManagerGatewayStatusCall(channel).run(request:request, metadata:metadata)
  }
  /// Asynchronous. Unary.
  internal func gatewaystatus(_ request: Router_GatewayStatusRequest,
                  completion: @escaping (Router_GatewayStatusResponse?, CallResult)->())
    throws
    -> Router_RouterManagerGatewayStatusCall {
      return try Router_RouterManagerGatewayStatusCall(channel).start(request:request,
                                                 metadata:metadata,
                                                 completion:completion)
  }
  /// Synchronous. Unary.
  internal func getstatus(_ request: Router_StatusRequest)
    throws
    -> Router_Status {
      return try Router_RouterManagerGetStatusCall(channel).run(request:request, metadata:metadata)
  }
  /// Asynchronous. Unary.
  internal func getstatus(_ request: Router_StatusRequest,
                  completion: @escaping (Router_Status?, CallResult)->())
    throws
    -> Router_RouterManagerGetStatusCall {
      return try Router_RouterManagerGetStatusCall(channel).start(request:request,
                                                 metadata:metadata,
                                                 completion:completion)
  }
}
