/*
 * DO NOT EDIT.
 *
 * Generated by the protocol buffer compiler.
 * Source: github.com/TheThingsNetwork/api/protocol/lorawan/device.proto
 *
 */

/*
 * Copyright 2017, gRPC Authors All rights reserved.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
import Foundation
import Dispatch
import gRPC

/// Type for errors thrown from generated server code.
internal enum Lorawan_DeviceManagerServerError : Error {
  case endOfStream
}

/// To build a server, implement a class that conforms to this protocol.
internal protocol Lorawan_DeviceManagerProvider {
  func getdevice(request : Lorawan_DeviceIdentifier, session : Lorawan_DeviceManagerGetDeviceSession) throws -> Lorawan_Device
  func setdevice(request : Lorawan_Device, session : Lorawan_DeviceManagerSetDeviceSession) throws -> Google_Protobuf_Empty
  func deletedevice(request : Lorawan_DeviceIdentifier, session : Lorawan_DeviceManagerDeleteDeviceSession) throws -> Google_Protobuf_Empty
}

/// Common properties available in each service session.
internal class Lorawan_DeviceManagerSession {
  fileprivate var handler : gRPC.Handler
  internal var requestMetadata : Metadata { return handler.requestMetadata }

  internal var statusCode : Int = 0
  internal var statusMessage : String = "OK"
  internal var initialMetadata : Metadata = Metadata()
  internal var trailingMetadata : Metadata = Metadata()

  fileprivate init(handler:gRPC.Handler) {
    self.handler = handler
  }
}

// GetDevice (Unary)
internal class Lorawan_DeviceManagerGetDeviceSession : Lorawan_DeviceManagerSession {
  private var provider : Lorawan_DeviceManagerProvider

  /// Create a session.
  fileprivate init(handler:gRPC.Handler, provider: Lorawan_DeviceManagerProvider) {
    self.provider = provider
    super.init(handler:handler)
  }

  /// Run the session. Internal.
  fileprivate func run(queue:DispatchQueue) throws {
    try handler.receiveMessage(initialMetadata:initialMetadata) {(requestData) in
      if let requestData = requestData {
        let requestMessage = try Lorawan_DeviceIdentifier(serializedData:requestData)
        let replyMessage = try self.provider.getdevice(request:requestMessage, session: self)
        try self.handler.sendResponse(message:replyMessage.serializedData(),
                                      statusCode:self.statusCode,
                                      statusMessage:self.statusMessage,
                                      trailingMetadata:self.trailingMetadata)
      }
    }
  }
}

// SetDevice (Unary)
internal class Lorawan_DeviceManagerSetDeviceSession : Lorawan_DeviceManagerSession {
  private var provider : Lorawan_DeviceManagerProvider

  /// Create a session.
  fileprivate init(handler:gRPC.Handler, provider: Lorawan_DeviceManagerProvider) {
    self.provider = provider
    super.init(handler:handler)
  }

  /// Run the session. Internal.
  fileprivate func run(queue:DispatchQueue) throws {
    try handler.receiveMessage(initialMetadata:initialMetadata) {(requestData) in
      if let requestData = requestData {
        let requestMessage = try Lorawan_Device(serializedData:requestData)
        let replyMessage = try self.provider.setdevice(request:requestMessage, session: self)
        try self.handler.sendResponse(message:replyMessage.serializedData(),
                                      statusCode:self.statusCode,
                                      statusMessage:self.statusMessage,
                                      trailingMetadata:self.trailingMetadata)
      }
    }
  }
}

// DeleteDevice (Unary)
internal class Lorawan_DeviceManagerDeleteDeviceSession : Lorawan_DeviceManagerSession {
  private var provider : Lorawan_DeviceManagerProvider

  /// Create a session.
  fileprivate init(handler:gRPC.Handler, provider: Lorawan_DeviceManagerProvider) {
    self.provider = provider
    super.init(handler:handler)
  }

  /// Run the session. Internal.
  fileprivate func run(queue:DispatchQueue) throws {
    try handler.receiveMessage(initialMetadata:initialMetadata) {(requestData) in
      if let requestData = requestData {
        let requestMessage = try Lorawan_DeviceIdentifier(serializedData:requestData)
        let replyMessage = try self.provider.deletedevice(request:requestMessage, session: self)
        try self.handler.sendResponse(message:replyMessage.serializedData(),
                                      statusCode:self.statusCode,
                                      statusMessage:self.statusMessage,
                                      trailingMetadata:self.trailingMetadata)
      }
    }
  }
}


/// Main server for generated service
internal class Lorawan_DeviceManagerServer {
  private var address: String
  private var server: gRPC.Server
  private var provider: Lorawan_DeviceManagerProvider?

  /// Create a server that accepts insecure connections.
  internal init(address:String,
              provider:Lorawan_DeviceManagerProvider) {
    gRPC.initialize()
    self.address = address
    self.provider = provider
    self.server = gRPC.Server(address:address)
  }

  /// Create a server that accepts secure connections.
  internal init?(address:String,
               certificateURL:URL,
               keyURL:URL,
               provider:Lorawan_DeviceManagerProvider) {
    gRPC.initialize()
    self.address = address
    self.provider = provider
    guard
      let certificate = try? String(contentsOf: certificateURL, encoding: .utf8),
      let key = try? String(contentsOf: keyURL, encoding: .utf8)
      else {
        return nil
    }
    self.server = gRPC.Server(address:address, key:key, certs:certificate)
  }

  /// Start the server.
  internal func start(queue:DispatchQueue = DispatchQueue.global()) {
    guard let provider = self.provider else {
      assert(false) // the server requires a provider
    }
    server.run {(handler) in
      print("Server received request to " + handler.host
        + " calling " + handler.method
        + " from " + handler.caller
        + " with " + String(describing:handler.requestMetadata) )

      do {
        switch handler.method {
        case "/lorawan.DeviceManager/GetDevice":
          try Lorawan_DeviceManagerGetDeviceSession(handler:handler, provider:provider).run(queue:queue)
        case "/lorawan.DeviceManager/SetDevice":
          try Lorawan_DeviceManagerSetDeviceSession(handler:handler, provider:provider).run(queue:queue)
        case "/lorawan.DeviceManager/DeleteDevice":
          try Lorawan_DeviceManagerDeleteDeviceSession(handler:handler, provider:provider).run(queue:queue)
        default:
          break // handle unknown requests
        }
      } catch (let error) {
        print("Server error: \(error)")
      }
    }
  }
}
